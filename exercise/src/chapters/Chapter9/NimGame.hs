module Nimgame
    (
    ) where

import Parsing -- if you run in ghci, then you have to do ":set -isrc/chapters/Chapter8"

-- todo Separate the module for console screen control from the Lifegame module and use it in common.